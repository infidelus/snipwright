"""Optional add-on features for Snipwright (e.g. the TMDB/TVDB renamer).

These modules are deliberately self-contained and free of Qt and third-party
dependencies so the core logic can be tested on its own and reused.
"""
